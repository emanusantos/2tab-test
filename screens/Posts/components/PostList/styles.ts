import EStyleSheet from "react-native-extended-stylesheet";

const styles = EStyleSheet.create({
  contentContainer: { paddingBottom: "5rem" },
  separator: { marginBottom: "1rem" },
  activity: { marginTop: "20%" },
});

export default styles;
